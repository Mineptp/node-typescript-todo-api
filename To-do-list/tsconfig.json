{
  "compilerOptions": {
    /* Cấu hình cơ bản */
    "target": "ES2020" /* Phiên bản JS đích */,
    "module": "NodeNext" /* Chuẩn module (quan trọng cho Node mới) */,
    "moduleResolution": "NodeNext" /* Cách tìm file (quan trọng) */,
    "outDir": "./dist" /* Thư mục chứa file sau khi build */,
    "rootDir": "./src" /* Thư mục chứa code nguồn */,

    /* Dòng này sẽ SỬA LỖI của bạn */
    "esModuleInterop": true /* Cho phép import module CJS (như Express) kiểu mặc định */,
    "resolveJsonModule": true, /* Kiểu mặc định cho file JSON (như package.json) */
    /* Các cấu hình kiểm tra lỗi */
    "strict": true /* Bật chế độ kiểm tra nghiêm ngặt */,
    "skipLibCheck": true /* Bỏ qua check lỗi trong file thư viện (để đỡ báo lỗi rác) */,
    "forceConsistentCasingInFileNames": true /* Bắt buộc viết đúng hoa thường tên file */
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
